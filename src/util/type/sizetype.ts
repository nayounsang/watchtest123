export interface defaultSizeType {
  width?: string;
  height?: string;
  fontSize?: string;
}

export interface sizeSelectType {
  tiny: defaultSizeType;
  small: defaultSizeType;
  medium: defaultSizeType;
  big: defaultSizeType;
  huge: defaultSizeType;
}
